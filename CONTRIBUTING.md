# 参与贡献

感谢帮助改进 KHive。提交问题或代码前，请先阅读 [README](README.md) 的功能范围及 [LICENSE](LICENSE)。贡献不会改变上游和第三方组件的许可条件。

## 问题反馈

请提供 KHive 版本、操作系统与架构、模组型号／固件、设备后端、复现步骤、预期行为与实际结果。只附与问题直接相关的脱敏日志。

不要上传真实配置、数据库、备份、短信正文、电话号码、IMSI、ICCID、EID、IMEI、eSIM 二维码或激活码、通知密钥、登录凭据、网络抓包或完整浏览器请求。需要展示身份关联时，使用一致的虚构标识。涉及安全漏洞时先与维护者约定私有披露渠道，不在普通 Issue 中公开可利用细节或凭据。

## 提交代码

1. 从目标版本或最新 `main` 建立功能分支，保持一个改动解决一个明确问题。
2. 沿用现有结构和依赖，避免无关重构或格式化；新增依赖说明必要性和许可证。
3. 为行为变化补充适当测试，夹具使用虚构数据。实机操作、切卡、发短信、写入 eUICC 不能成为普通测试的隐式步骤。
4. 按 [开发与检查](docs/DEVELOPMENT.md) 验证，并在 PR 中区分构建、隔离测试和实机结果。没有完成的验证明确标注。
5. 暂存准备提交的文件后执行 `make check-release`，检查暂存差异，确认不含运行数据、个人路径及敏感信息。

公开 API、配置和数据结构的变化请同步文档，并说明兼容性影响。保留现有版权与许可证。不要将生成的前端资源、二进制、依赖目录、实机记录或 Docker 部署文件加入源码提交。

## 版本

版本采用 `v主版本.次版本.修订版本` 标签。正式版本以对应标签的源码为准；发行说明描述用户可见能力、适用范围和必要的兼容性变化。开发过程、现场日志和个人部署步骤不放入发行说明。
