# 版本说明

## 1.0.1 — 2026-09-14

- 代理探测增加真实 SOCKS5 UDP DNS 往返验证，区分 UDP 协商成功与实际转发成功，并返回探测阶段及 DNS 结果。
- 入站短信与各通知渠道的待发记录在同一事务中持久化；增加退避重试、重启恢复、投递记录查询及失败记录手动重试。通知采用至少一次投递语义，外部响应丢失时仍可能重复。
- 短信提交状态与终端送达状态分开记录，增加持久化状态报告、按分片关联和会话内回执展示。支持报告早到、重复和乱序处理，按 SIM、完整收件号码、引用号与时间窗口匹配，歧义报告不强行关联。
- AT 回执优先使用直接 `+CDS`，不支持时依次回退到存储报告及普通短信；统一启动、VoWiFi 退出、SIM 重载和发送前的模式协商。支持报告与命令响应交错，并在需要确认的模组模式下先持久化再确认。
- 运营商规则可按 IMSI／ICCID 前缀、SPN、GID1／GID2 区分同 PLMN 下的不同 SIM，保留原有 PLMN 回退，拒绝并列歧义。选定配置在同一次连接中保持一致。
- 将通用 IMS 注册模板命名为 `default-ims-register`，避免把模板名称误认为当前运营商；协议参数保持不变。

**兼容性：** 启动时新增通知、状态报告数据表及短信分片字段，升级前应备份数据库。保留原有短信提交状态和查询接口格式；历史短信不会补发通知或被自动标记送达。回退应使用对应版本的程序、配置和升级前数据库备份，并保留升级后的数据供核对。

**验收范围：** Linux amd64 的相关隔离回归、真实代理 UDP 往返、VoWiFi 入站短信与 Telegram API 成功响应，以及 AT 直接终端回执已验证；同一开发版本的服务重启、切卡和 VoWiFi 退出后的回执模式恢复已通过实机检查。运营商是否返回报告取决于实际线路；QMI／MBIM 发送路径尚未新增终端引用号追踪，PC/SC 与完整语音不属于本次改动。

## 1.0.0 — 2026-09-13

KHive 首个正式版本，提供统一的蜂窝模组、SIM／eSIM、短信和网络管理界面。

- 设备发现、状态概览、热插拔恢复与按卡保存的网络策略。
- eSIM Profile 管理、二维码照片导入、操作互斥、会话失效保护和通知收尾。
- 短信会话、中文长短信持久化重组及更明确的发送状态。
- VoWiFi／IMS 短信、USSD、注册保活和连接恢复。
- 消息通知、SOCKS5／HTTP 代理实例及 AT／ECM IPv4 数据管理。
- 内嵌 Web 界面的 Linux 程序，提供源码编译、配置和使用说明。

**范围：** Linux amd64 为当前发布基线；其他架构提供交叉编译目标。语音未纳入已验收能力，ECM 暂不支持 IPv6 和运营商公网 IP 轮换。没有 Docker 部署方案或自动更新源。

**接口：** eSIM 下载使用 POST JSON。使用旧 GET 下载调用的脚本需要更新。

具体要求、能力边界与许可条件见 [README](README.md)。
